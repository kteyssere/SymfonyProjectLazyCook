{% extends 'base.html.twig' %}

{% block title %}Modifier le profil{% endblock %}

{% block body %}

    {{ include('user/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    <div style="text-align: center;">
        <a href="{{ path('app_user_show', {id: app.user.id}) }}" class="btn" style="padding: 10px; background-color: black; color: white; font-size: 14px; width: 170px; margin-bottom: 5px;">Annuler</a>
        {{ include('user/_delete_form.html.twig') }}
    </div>

{% endblock %}
