{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1>{{ category.name }}</h1>

{#    <table class="table">#}
{#        <tbody>#}
{#            <tr>#}
{#                <th>Id</th>#}
{#                <td>{{ category.id }}</td>#}
{#            </tr>#}
{#            <tr>#}
{#                <th>Name</th>#}
{#                <td>{{ category.name }}</td>#}
{#            </tr>#}
{#        </tbody>#}
{#    </table>#}

    {% for recipe in recipes %}
    <div>
        <p>{{ recipe.picture }}</p>
        <p>{{ recipe.name }}</p>
    </div>

{% else %}
    <p>Aucune recette pour cette catégorie</p>
    {% endfor %}
    <a href="{{ path('app_category_index') }}">back to list</a>

    <a href="{{ path('app_category_edit', {'id': category.id}) }}">edit</a>

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
