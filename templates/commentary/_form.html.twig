{{ form_start(form) }}

    <div style="padding: 15px;">
        <p style="font-weight: bold;">{{ commentary.user.name }} le {{ commentary.datepublicom |date("d/m/Y")}} à {{ commentary.datepublicom |date("H:i")}}:</p>
        <p style="font-style: italic;">{{ form_widget(form.content) }}</p>
    </div>
<button class="btn" style="background-color: green; color: white; margin-left: 20px; width: 130px; margin-bottom: 5px;">{{ button_label|default('Save') }}</button>

{{ form_end(form) }}
